process {
    withName: BWA_MEM {
        ext.prefix = { "${meta.id}.bwa.mem" }
        ext.args = '-5SP'
    }

    withName: SAMBLASTER {
        ext.prefix = { "${meta.id}.samblaster" }
        ext.args3 = '-h -F 2316'
    }
}
