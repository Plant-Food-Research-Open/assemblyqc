params { 
    inputFilePath       = "$PWD/input_data/test_sample.fa"
    lineage_dataset     = "embryophyta_odb10"
    augustus_species    = "arabidopsis"
    mode                = "geno"
    
    threads             = 16
    help                = false
    outdir {
        main            = "$PWD/results"
    }
}

process {
    executor            = 'slurm'
    cpus                = 4
    time                = '1hour'
    memory              = '4.GB'
}

singularity {
    enabled             = true
    autoMounts          = true
}