params { 
    input_files             = [["hap1", "$launchDir/input_data/test_data.fasta"], ["hap2", "$launchDir/input_data/test_data2.fasta"]]
    lineage_datasets        = ["fungi_odb10", "microsporidia_odb10"]
    augustus_species        = "fumigatus"
    mode                    = "geno"
    
    outdir {
        main                = "$launchDir/results"
    }
}

process {
    executor                = 'slurm'
    cpus                    = 16
    time                    = '1hour'
    memory                  = '8.GB'
}

singularity {
    enabled                 = true
    autoMounts              = true
}