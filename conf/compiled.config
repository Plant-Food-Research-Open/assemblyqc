params {
    // Complied variables. Don't modify!
    ht_factor               = ht_fact(is_hyperthreaded)
}

def ht_fact(is_hyperthreaded) {
    if (is_hyperthreaded != 0) {
        return 2
    } else {
        return 1
    }
}